fastapi==0.109.0
uvicorn==0.27.0
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyroute2==0.7.9
aiosqlite==0.19.0
websockets==12.0
psutil==5.9.7

# Direct imports that were relying on being pulled in by something else.
# pydantic arrives with fastapi today, but three modules import it directly and
# a transitive dependency is not a promise. numpy was simply undeclared, so a
# clean install left ffn_sandbox_agent unimportable.
numpy
pydantic

# FastAPI needs this to build any route taking Form or UploadFile. It is not a
# soft dependency: /api/license/upload declares one, so fastapi raises at IMPORT
# time and ffn_manager cannot be loaded at all without it. A clean install of this
# file was unimportable until CI tried it -- the same trap the note above
# describes, one dependency further on.
python-multipart
